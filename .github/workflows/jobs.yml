on: 
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm install 
          npm run build 
        shell: bash

      - run: print %ENV
        shell: perl {0}
