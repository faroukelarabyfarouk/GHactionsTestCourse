on: 
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: |
          npm install
          npm test
        shell: bash

      - run: print %ENV
        shell: perl {0}
